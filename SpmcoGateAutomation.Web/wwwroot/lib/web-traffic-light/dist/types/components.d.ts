/* eslint-disable */
/* tslint:disable */
/**
 * This is an autogenerated file created by the Stencil compiler.
 * It contains typing information for all components that exist in this project.
 */
import { HTMLStencilElement, JSXBase } from "./stencil-public-runtime";
import { TrafficLightState } from "./components/traffic-light/traffic-light-state";
import { TrafficLightColor } from "./components/traffic-light/traffic-light-color";
import { TrafficLightMode } from "./components/traffic-light/traffic-light-mode";
export namespace Components {
    interface TrafficLight {
        /**
          * The color of the light that should be turned on. This is ignored when `current-state` is off or when in `three-lights` mode and with `current-state` set to `all-on`.
         */
        "color": TrafficLightColor;
        /**
          * Specifies whether lights should be turned on or off. It defaults to `off`.  Setting it to `on` will turn on the light specified in the `color` property.  Setting it to `all-on` will turn on all the lights when `mode` is set to `three-lights`. When in `single-light` mode, setting the `current-state` to `all-on` has the same effect as setting it to `on` (turning on the single light).
         */
        "currentState": TrafficLightState;
        /**
          * Mode in which the component is displayed. It defaults to `three-lights-mode`.
         */
        "mode": TrafficLightMode;
    }
}
declare global {
    interface HTMLTrafficLightElement extends Components.TrafficLight, HTMLStencilElement {
    }
    var HTMLTrafficLightElement: {
        prototype: HTMLTrafficLightElement;
        new (): HTMLTrafficLightElement;
    };
    interface HTMLElementTagNameMap {
        "traffic-light": HTMLTrafficLightElement;
    }
}
declare namespace LocalJSX {
    interface TrafficLight {
        /**
          * The color of the light that should be turned on. This is ignored when `current-state` is off or when in `three-lights` mode and with `current-state` set to `all-on`.
         */
        "color"?: TrafficLightColor;
        /**
          * Specifies whether lights should be turned on or off. It defaults to `off`.  Setting it to `on` will turn on the light specified in the `color` property.  Setting it to `all-on` will turn on all the lights when `mode` is set to `three-lights`. When in `single-light` mode, setting the `current-state` to `all-on` has the same effect as setting it to `on` (turning on the single light).
         */
        "currentState"?: TrafficLightState;
        /**
          * Mode in which the component is displayed. It defaults to `three-lights-mode`.
         */
        "mode"?: TrafficLightMode;
    }
    interface IntrinsicElements {
        "traffic-light": TrafficLight;
    }
}
export { LocalJSX as JSX };
declare module "@stencil/core" {
    export namespace JSX {
        interface IntrinsicElements {
            "traffic-light": LocalJSX.TrafficLight & JSXBase.HTMLAttributes<HTMLTrafficLightElement>;
        }
    }
}
